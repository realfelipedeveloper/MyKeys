{
  "name": "shared",
  "projectType": "library",
  "sourceRoot": "packages/shared/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/shared"],
      "options": {
        "command": "pnpm exec tsc -p packages/shared/tsconfig.lib.json && node tools/build-package.mjs shared"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec eslint packages/shared"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec tsc --noEmit -p packages/shared/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/check-packages.mjs shared --test"
      }
    }
  },
  "tags": ["scope:shared", "type:package"]
}
